@{
    ViewData["Title"] = "Upload Photo";
}

<h2>Upload Photo</h2>

<form asp-action="Upload" method="post" enctype="multipart/form-data">
    <div class="mb-3">
        <label for="photoFile" class="form-label">Photo file</label>
        <input id="photoFile" type="file" name="photoFile" class="form-control" />
        
    </div>

    <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <input id="description" type="text" name="description" class="form-control" />
    </div>

    <div class="mb-3">
        <label for="hashtags" class="form-label">Hashtags</label>
        <input id="hashtags" type="text" name="hashtags" class="form-control" />
    </div>

    <div class="mb-3">
        <label for="processingOption" class="form-label">Processing</label>
        <select id="processingOption" name="processingOption" class="form-select">
            <option value="Original">Original</option>
            <option value="Resize800">Resize to 800px</option>
            <option value="Grayscale">Grayscale</option>
        </select>
    </div>

    <button type="submit" class="btn btn-primary">Upload</button>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
